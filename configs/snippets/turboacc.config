# TurboACC — applied after base defconfig (workflow Defconfig and TurboACC step).
# LuCI app has no kmod LUCI_DEPENDS (Kconfig cycle fix); enable kmods here explicitly.
CONFIG_PACKAGE_luci-app-turboacc=y
CONFIG_PACKAGE_kmod-nft-fullcone=y
CONFIG_PACKAGE_kmod-tcp-bbr=y
