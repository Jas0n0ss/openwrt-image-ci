name: Build builder Docker image

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - docker/**
      - .github/workflows/docker-builder-image.yml

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-builder-image
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: image
        run: |
          base="ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')/builder"
          echo "base=$base" >> "$GITHUB_OUTPUT"

      - uses: docker/build-push-action@v6
        with:
          context: docker
          push: true
          tags: |
            ${{ steps.image.outputs.base }}:22.04
            ${{ steps.image.outputs.base }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image summary
        run: |
          echo "Published:"
          echo "  ${{ steps.image.outputs.base }}:22.04"
          echo "  ${{ steps.image.outputs.base }}:latest"
